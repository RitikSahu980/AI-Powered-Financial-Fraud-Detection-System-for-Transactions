package com.frauddetection.backend.enums;

/** Human-readable classification label, derived from the ML service's raw 0/1 output. */
public enum PredictionLabel {
    FRAUDULENT,
    NOT_FRAUDULENT
}
