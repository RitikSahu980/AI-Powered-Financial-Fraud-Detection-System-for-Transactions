# Base URL of the Spring Boot backend's API (Module 7's /api/v1 prefix).
# In production this should point at wherever the Node.js API Gateway
# fronts the Spring Boot backend, per the frozen architecture - this
# frontend never calls the Python ML service or MongoDB directly.
VITE_API_BASE_URL=http://localhost:8080/api/v1
